services:
  pseudocode-dev:
    container_name: pseudocode-dev
    build: 
      context: .
      dockerfile: Dockerfile

    volumes:
      - ../..:/workspaces:cached

    environment:
      DOTNET_CLI_TELEMETRY_OPTOUT: "1"

    command: sleep infinity
